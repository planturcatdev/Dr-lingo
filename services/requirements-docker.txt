# Docker Container Requirements - Celery Worker/Beat/Flower

# Django Core (required - tasks import Django models)
Django>=4.2,<5.0
djangorestframework>=3.14.0
djangorestframework-simplejwt>=5.3.0

# Database (required - tasks access database)
psycopg2-binary>=2.9.0
dj-database-url>=2.1.0

# Configuration
python-decouple>=3.8

# API Documentation (required - in INSTALLED_APPS)
drf-yasg>=1.21.7

# AI Services (Gemini fallback)
google-generativeai>=0.3.0

# Celery Task Queue (core requirement)
celery[redis]>=5.3.0
django-celery-results>=2.5.0
django-celery-beat>=2.5.0

# Redis (broker + cache)
redis>=5.0.0
django-redis>=5.4.0

# RabbitMQ Event Bus
pika>=1.3.0

# Flower Monitoring
flower>=2.0.0

# Media Processing (for audio/image file handling)
Pillow>=10.0.0
python-magic>=0.4.27

# PDF Processing (text extraction)
pypdf>=4.0.0
# pdf2image and pytesseract require system packages (poppler, tesseract)
# Uncomment if OCR is needed in Docker:
# pdf2image>=1.17.0
# pytesseract>=0.3.13

# Two-Factor Authentication (OTP)
django-otp>=1.5.0
django-two-factor-auth>=1.16.0
qrcode>=7.4.2

# CORS
django-cors-headers>=4.0.0

# Hugging Face Datasets (no audio extra to avoid torch)
# Uncomment if dataset import is needed in Docker:
# datasets>=3.6.0
# huggingface-hub>=0.25.0
