# Docker Container Requirements - Celery Worker/Beat/Flower

# Django Core (required - tasks import Django models)
Django>=4.2,<5.0
djangorestframework>=3.14.0

# Database (required - tasks access database)
psycopg2-binary>=2.9.0
dj-database-url>=2.1.0

# Configuration
python-decouple>=3.8

# API Documentation (required - in INSTALLED_APPS)
drf-yasg>=1.21.7

# AI Services (Gemini fallback)
google-generativeai>=0.3.0

# Celery Task Queue (core requirement)
celery[redis]>=5.3.0
django-celery-results>=2.5.0
django-celery-beat>=2.5.0

# Redis (broker + cache)
redis>=5.0.0
django-redis>=5.4.0

# RabbitMQ Event Bus
pika>=1.3.0

# Flower Monitoring (only needed for flower container, but small)
flower>=2.0.0

# HTTP Client (for Ollama/Whisper API calls)
requests>=2.31.0

# Media Processing (for audio file handling)
Pillow>=10.0.0
python-magic>=0.4.27

# Two-Factor Authentication (OTP)
django-otp>=1.5.0
django-two-factor-auth>=1.16.0
qrcode>=7.4.2

# JWT Auth
djangorestframework-simplejwt>=5.3.0

# CORS (minimal, needed if tasks touch request context)
django-cors-headers>=4.0.0

# Text-to-Speech (Coqui TTS with XTTS v2)
# WARNING: This pulls in torch (~900MB) - image will be ~3GB
# Model cache is mounted from host (~/.local/share/tts) to avoid re-downloading
TTS>=0.22.0
